<Window x:Class="RecipeControl.Views.TestConnectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Test de Conexión" Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDC3C7"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#27AE60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ResultBox" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D5DBDB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="Test de Conexión del Sistema" 
                   Style="{StaticResource TitleStyle}"/>

        <!-- Contenido Principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Sección Base de Datos -->
                <TextBlock Text="🗄️ Base de Datos" Style="{StaticResource SectionTitle}"/>

                <Border Style="{StaticResource ResultBox}">
                    <StackPanel>
                        <TextBlock x:Name="txtResultadoDB" 
                                   TextWrapping="Wrap" 
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   Foreground="#2C3E50"
                                   Text="Presione 'Probar Conexión BD' para verificar la conexión a SQL Server"/>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Content="Probar Conexión BD" 
                                    Style="{StaticResource ButtonStyle}"
                                    Click="BtnProbarDB_Click"/>
                            <Button x:Name="btnCrearTablas"
                                    Content="Crear Tablas" 
                                    Style="{StaticResource SuccessButton}"
                                    IsEnabled="False"/>
                        </StackPanel>

                        <!-- Progress Bar -->
                        <ProgressBar x:Name="progressDB" 
                                     Height="4" 
                                     Margin="0,10,0,0"
                                     IsIndeterminate="False"
                                     Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <!-- Sección Puerto Serial -->
                <TextBlock Text="🔌 Puerto Serial (RS-232)" Style="{StaticResource SectionTitle}"/>

                <Border Style="{StaticResource ResultBox}">
                    <StackPanel>
                        <TextBlock x:Name="txtResultadoSerial" 
                                   TextWrapping="Wrap" 
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   Foreground="#2C3E50"
                                   Text="Presione 'Probar Puerto Serial' para verificar la comunicación con la balanza"/>

                        <Button Content="Probar Puerto Serial" 
                                Margin="0,10,0,0"
                                Style="{StaticResource ButtonStyle}"/>

                        <!-- Progress Bar -->
                        <ProgressBar x:Name="progressSerial" 
                                     Height="4" 
                                     Margin="0,10,0,0"
                                     IsIndeterminate="False"
                                     Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <!-- Sección Configuración Actual -->
                <TextBlock Text="⚙️ Configuración Actual" Style="{StaticResource SectionTitle}"/>

                <Border Style="{StaticResource ResultBox}">
                    <TextBlock x:Name="txtConfiguracion" 
                               TextWrapping="Wrap" 
                               FontFamily="Consolas"
                               FontSize="11"
                               Foreground="#7F8C8D"/>
                </Border>
                <Button Content="Button"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Botones Inferiores -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Probar Todo" 
                    Style="{StaticResource SuccessButton}"/>
            <Button Content="Cerrar" 
                    Style="{StaticResource ButtonStyle}"
                    Background="#95A5A6"/>
        </StackPanel>
    </Grid>
</Window>
